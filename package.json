{
  "name": "afa-website",
  "version": "1.0.0",
  "description": "Anti-Fraud Alliance — site static cu teste Playwright",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
